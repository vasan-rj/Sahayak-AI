"""Sahayak proxy — FastAPI websocket proxy between the browser and the Gemini Live API.

The perception is probabilistic; the record is deterministic. This package owns the
deterministic half: session lifecycle, event logging, and the append-only witness log.
"""
